{
  "name": "pulsetriage",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c teal,magenta \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "install:all": "npm install --prefix api && npm install --prefix web",
    "seed": "npm run seed --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
